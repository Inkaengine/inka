CI runs the admin typing specs (inline editing, formatting, clipboard, links, markdown shortcuts, slash menu, block add/remove) against the Next.js example, whose DOM persists between renders like a real React frontend's, in a job parallel to admin-mock. The test helper finds a page's main-content blocks from the bridge's blockPathMap instead of assuming the frontend wraps them in a <main>.
