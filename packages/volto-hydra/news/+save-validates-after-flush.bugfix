Saving validates the page with the text just typed on the canvas. Canvas edits reach the form after a short debounce, and the save flushed them only after validating, so a required field typed on a new block and saved straight away was refused with "Required input is missing" while the sidebar showed its value. The form now awaits the iframe's flush (`flushEditsRef`, the same flush the template save uses) before it validates.
