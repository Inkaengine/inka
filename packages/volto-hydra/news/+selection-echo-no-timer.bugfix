The admin hears about every caret move once it has placed the caret itself — straight away, and whether the author, a script or assistive technology moved it. The bridge used to stop reporting caret moves for 100ms after each placement (a Delete right after selecting could act on the old selection), and a placement's leftover timer could let a stale position through during the next one's render.
