{
  "name": "test-svelte",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:test": "vite --port ${HYDRA_SVELTE_DOC_PORT:?HYDRA_SVELTE_DOC_PORT must be set (tests-playwright/ports.ts has no defaults)} --strictPort"
  }
}
